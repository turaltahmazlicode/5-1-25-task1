@model List<Department>
<div class="main-panel">
    <div class="content-wrapper">
        <div class="col-lg-12 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Departments</h4>
                    <div class="table-responsive">
                        <table class="table table">
                            <a class="btn btn-warning" asp-action="Create">Create</a>
                            <thead>
                                <tr>
                                    <th> # </th>
                                    <th> Id </th>
                                    <th> Title </th>
                                </tr>
                            </thead>
                            <tbody>
                                @{
                                    int count = 1;
                                }
                                @foreach (Department department in Model)
                                {
                                    <tr>
                                        <td> @count </td>
                                        <td> @department.Id </td>
                                        <td> @department.Title </td>
                                        <td>
                                            <a class="btn btn-primary" asp-action="Edit" asp-route-id="@department.Id">Edit</a>
                                            <a class="btn btn-secondary" asp-action="Details" asp-route-id="@department.Id">Details</a>
                                            <a class="btn btn-danger" asp-action="Delete" asp-route-id="@department.Id">Delete</a>
                                        </td>
                                    </tr>
                                    count++;
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

